Duckweed Collaboration
======================

To run:

mvn package gae:run

To package:

mvn package gae:deploy

To run integration tests:

in one window run -
mvn gae:deploy

and in another run -
mvn verify
